#include "dialog.h"

#include <QtGui>
#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    dialog w;
    w.setWindowIconText("hello");
    w.setWindowTitle("Are you sure£¿");
    w.
    w.show();
    return a.exec();
}
